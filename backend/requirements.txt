flask-cors
tensorflow==2.12.0  # Adjust to the TensorFlow version you're using
keras==2.12.0       # Ensure it's compatible with TensorFlow
numpy==1.24.3       # For numerical computations
pandas==2.0.3       # For data manipulation and analysis
matplotlib==3.7.1   # For plotting graphs (optional if visualization is used)
scikit-learn==1.2.2 # For preprocessing and other utilities
h5py==3.8.0         # For saving/loading Keras models in HDF5 format
Pillow==9.5.0       # For image preprocessing (if dealing with images)
Flask==2.3.2        # Flask for web application development
